pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        maven {
            //jitpack
            url "https://jitpack.io"
        }
        maven {
            //pangle url
            url "https://artifact.bytedance.com/repository/pangle"
        }

        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            //jitpack
            url "https://jitpack.io"
        }
        maven {
            //pangle url
            url "https://artifact.bytedance.com/repository/pangle"
        }
    }
}
rootProject.name = "FFShare"
include ':app'
